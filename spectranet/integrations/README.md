# integrations/

Status: NOT YET IMPLEMENTED. Placeholder created in Phase 1 (platform boundary)
to establish the architectural separation between the existing ML platform
and the new AI-native platform layers, per the phased build plan.

See the top-level docs/architecture.md (once written) for how this layer
fits into the overall system.
